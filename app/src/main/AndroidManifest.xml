<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.loosash.learnanimatior">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TweenAnimationActivity" />
        <activity android:name=".tween.TweenTranslateActivity" />
        <activity android:name=".tween.TweenActivity" />
        <activity android:name=".tween.TweenScaleActivity" />
        <activity android:name=".tween.TweenRotateActivity" />
        <activity android:name=".tween.TweenAlphaActivity" />
        <activity android:name=".tween.TweenSetActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".property.PropertyActivity" />
        <activity android:name=".property.PropertyTranslateActivity"></activity>
    </application>

</manifest>